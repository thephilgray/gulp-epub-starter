extends ../templates/layout
append config
    - var main = 'Custom'
    - var epubTypes = ['titlepage']
    - var hasHeader = false

block content
    .titlepage
        .content
            .grid
                .row
                    .span-12-of-12.align-center
                        h1(epub:type='covertitle')=main
                        h2=subtitle
append scripts
    script
        script(src='../script/main.js', type='text/javascript')